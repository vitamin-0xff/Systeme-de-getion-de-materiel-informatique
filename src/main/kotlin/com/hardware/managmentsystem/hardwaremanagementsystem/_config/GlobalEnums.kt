package com.hardware.managmentsystem.hardwaremanagementsystem._config

enum class HardwareStatus { WORKING, BROKEN, MAINTENANCE }
enum class ComputerType { SERVER, PC, WORKSTATION, DESKTOP, LAPTOP }
enum class SizeUnit { GB, TB }
enum class FreqUnit { GHz, MHz }
enum class PeripheralType { SCREEN, KEYBOARD, MOUSE }
enum class KeyboardLayout { QWERTY, AZERTY }
enum class KeyboardSwitch { NORMAL, MECHANICAL }
enum class MarquesType {SCREEN, KEYBOARD, MOUSE, COMPUTER, SERVER, GPU, CPU, HARD_DRIVE, MEMORY, NETWORK, MOTHERBOARD, POWER_SUPPLY}